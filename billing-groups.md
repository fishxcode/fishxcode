---
title: 站点计费规则与分组简介
description: FishXCode 站点计费口径、倍率、令牌分组、定价页读法和套餐联动说明。
---

# 站点计费规则与分组简介

这页解释 FishXCode 的计费口径、令牌分组和套餐页面之间的关系。你可以把它当成购买和创建 Key 前的检查清单：先看定价页确认模型和套餐，再看分组决定 Key 能调用什么，最后用使用日志和分组健康状态核对实际消耗。

::: tip 当前口径
当前定价页口径为 **49 个模型、14 个可选分组**。模型、价格、分组和可售状态会随上游资源调整，最终以 [定价页](https://fishxcode.com/pricing?currency=CNY) 和控制台展示为准。
:::

## 截图对应模块

| 截图 | 对应问题 |
| --- | --- |
| 订阅套餐列表 | 套餐卡片里哪些字段影响计费 |
| 按次套餐权益 | 日上限、总次数、重置时间、适用模型怎么读 |
| 购买订阅套餐 | 支付前要核对哪些交付和有效期信息 |
| 创建令牌 | 分组如何绑定到 API Key |
| 基于套餐创建令牌 | 团队如何拆分套餐额度和子 Key |
| 令牌列表 | 如何确认 Key 的分组、倍率、模型和过期时间 |
| 分组健康状态 | 使用后如何核对消耗、缓存和异常范围 |

## 一、先区分三种计费入口

FishXCode 的费用不是单一规则。实际使用时，先判断你在用哪种入口：

| 入口 | 看什么 | 适合什么场景 |
| --- | --- | --- |
| 按量使用 | 模型基础价格、输入/输出/缓存倍率、令牌分组 | 按余额调用 API、临时测试、多模型混用 |
| 订阅套餐 | 有效期、总额度、每日上限、每月上限、适用分组 | 团队固定预算、Codex / Claude 系列长期使用 |
| 令牌分组 | 这个 Key 可以调用哪些模型、走哪个资源池、按什么倍率扣费 | 创建 Key、拆分项目、团队成本归因 |

相关套餐规则可以和 [订阅套餐总览](/subscription-plans) 一起看：本页解释“模型、倍率、分组如何影响扣费”，套餐页解释“购买后额度、有效期、次数和上限如何生效”。

## 二、定价页模块：先看套餐卡片

定价页的套餐卡片适合快速判断：这个套餐给多少额度或次数、多久有效、每天能用多少、是否有特殊折算规则。

![订阅套餐列表示例](/img/plans/subscription-plans.png)

读套餐卡片时重点看这些字段：

| 字段 | 含义 | 怎么判断 |
| --- | --- | --- |
| 当前价格 | 本次购买价格和有效期 | 例如 `¥320.00 / 1个月` 表示一个月价格 |
| 单次成本 | 套餐价格除以总次数后的参考成本 | 用来比较同类按次套餐性价比 |
| 套餐上限 | 当前套餐总额度或总次数 | 例如 `15000 次` 表示本套餐最多累计 15000 次基础请求 |
| 每天上限 | 单日最多可用额度或次数 | 例如 `500 次/天`，当天达到后通常需要等重置 |
| 重置时间 | 周期上限恢复时间 | 例如 `每天 08:00:00 重置` |
| 适用范围 | 套餐绑定的分组、模型和供应商 | 例如 `sub_plan_claude_mini_plus · 9 模型 · 2 供应商` |
| 按次计费说明 | 特殊模型是否按倍率折算次数 | 例如 Claude Opus 4.8、GPT-5.5 按 `2.5 * 1 次` 计算 |

::: tip 例子：按次套餐怎么扣
某套餐写着 `500次/天，月共15000次`，并说明 `Claude Opus 4.8、GPT-5.5 按 2.5 * 1 次计算`。

普通模型成功请求 1 次，通常记 1 次；Claude Opus 4.8 或 GPT-5.5 成功请求 1 次，会按 2.5 次计入套餐用量。也就是说，当天如果一直使用这类高倍率模型，约 200 次成功请求就可能达到 `500次/天` 的日上限。
:::

## 三、套餐权益模块：看清上限、重置和适用模型

打开套餐权益或详情页后，可以看到更细的权益字段。这个模块用于确认“买了之后到底能用什么”。

![按次套餐权益示例](/img/plans/by-number-of-times.png)

这张图里可以看到：

- **每天上限**：单日最多可用次数。
- **套餐上限**：整个有效期内总次数。
- **什么时候重置**：日上限的恢复时间。
- **开放购买时间**：某些套餐只在指定时间段开放购买。
- **供应商**：套餐覆盖的上游供应商。
- **可用模型**：购买后该套餐分组能调用的模型列表。
- **升级分组**：购买后自动进入或绑定的分组，例如 `sub_plan_claude_mini_max`。

::: warning 套餐页和分组页要一起看
套餐页告诉你“额度和上限”，分组告诉你“模型和通道”。如果套餐显示支持某个升级分组，但你的实际 Key 没绑定到该套餐或该分组，请求仍可能走余额或报权限错误。
:::

## 四、购买模块：支付前核对有效期和开通方式

点击购买后会进入购买弹窗。这个模块用于最后确认：买哪个套餐、多少钱、有效期多长、是否自动开通。

![购买订阅套餐示例](/img/plans/buyplan.png)

购买前重点核对：

| 字段 | 为什么重要 |
| --- | --- |
| 当前价格 | 判断本次支付金额，避免选错套餐 |
| 有效期 | 到期后套餐权益会失效 |
| Token 额度 / 次数 | 决定本套餐可消耗的总量 |
| 套餐类型 | 自动开通、人工发放或其他交付方式会影响生效时间 |
| 支付方式 | 确认支付宝、微信或其他支付入口 |
| 购买须知 | 退款、开通、限购和售后规则以页面展示为准 |

如果你只想临时测试模型，不一定要先买套餐，也可以先用余额按量调用。

## 五、按量使用：用余额直接调用模型

按量使用就是不先购买固定套餐，直接用账户余额调用模型。它适合先跑通工具、低频调用、多模型混用，或者还不确定要买哪类套餐的情况。

按量使用的基本路径：

1. 在 [定价页](https://fishxcode.com/pricing?currency=CNY) 确认目标模型支持哪些分组。
2. 在 **控制台 → 令牌管理** 创建普通令牌，选择合适的令牌分组。
3. 把 Key 配置到 Claude Code、Codex、Cursor、RooCode 或自有程序。
4. 每次成功请求后，系统按模型价格、输入/输出 Token、缓存命中和分组倍率扣减余额。
5. 在 **控制台 → 使用日志** 核对实际消耗。

按量扣费可以理解为：

```text
实际消耗 ≈ 模型基础消耗 × 分组倍率 + 输出/缓存等差异项
```

这里的“模型基础消耗”不是固定值，会随输入长度、输出长度、缓存命中、模型本身价格变化。图片、视频等固定价格模型不适合用普通文本 Token 方式估算。

余额按量和套餐的区别：

| 类型 | 特点 | 适合场景 |
| --- | --- | --- |
| 余额按量 | 用多少扣多少，余额不足时停止调用 | 临时测试、多模型混用、低频使用 |
| Codex 套餐 | 通常按有效期和额度管理 | Codex / GPT 编程任务、团队固定预算 |
| 按次套餐 | 按成功请求次数累计，并可能有日上限 | 想控制请求次数预算 |
| ClaudeMax 套餐 | 偏账号权益和 Claude Code 使用路径 | Claude Code 官方号订阅场景 |

::: tip 例子：按量更适合什么
如果你只是想测试某个工具能不能连通 FishXCode，先用 `default` 或目标模型推荐分组创建普通令牌，再发起少量请求即可。确认模型、分组和客户端配置都正确后，再根据用量决定是否购买 Codex 套餐、按次套餐或 ClaudeMax 套餐。
:::

::: warning 按量也受分组限制
账户有余额不代表任何 Key 都能调用任何模型。模型是否可用仍取决于令牌分组。如果报 `model not found`、`model not available` 或权限错误，先检查 Key 的分组是否包含目标模型。
:::

## 六、令牌模块：分组决定 Key 的模型范围

令牌分组可以理解为 **这个 API Key 的可用模型范围 + 扣费倍率规则 + 上游资源池**。创建 Key 时一定要选对分组。

![创建令牌时选择分组](/img/start/api-06-token-create.png)

创建普通令牌时重点看：

- **令牌分组**：决定这个 Key 能调用哪些模型。
- **额度设置**：限制当前 Key 最多可消耗多少额度；无限额度不代表不扣费，仍受账户余额或套餐限制。
- **模型限制列表**：非必要不建议限制，除非你明确只允许某几个模型。
- **IP 白名单**：适合服务端固定出口；个人本地开发慎用，避免 IP 变化后无法调用。

分组选错时，常见表现包括：

- 模型列表里看得到模型，但当前 Key 调用时报 `model not found`、`model not available` 或权限错误。
- 能调用模型，但扣费倍率、缓存表现或稳定性不是预期的那一档。
- 套餐 Key 和普通余额 Key 混用，导致额度走错账户或统计口径不清晰。

## 七、套餐令牌模块：基于套餐创建团队 Key

购买套餐后，系统可能自动生成套餐访问令牌，也可以基于套餐创建新的团队令牌。这样做适合多人共享套餐额度，但又需要区分成员或项目消耗。

![基于套餐创建令牌示例](/img/plans/create.png)

基于套餐创建令牌时，重点看这几项：

| 字段 | 说明 |
| --- | --- |
| 基于套餐创建 | 选择要绑定的套餐；绑定后到期时间、分组和模型限制会跟随套餐 |
| 名称 | 建议按成员、项目或服务命名，方便日志筛选 |
| 令牌分组 | 默认跟随套餐或用户分组；不要随意切到不匹配的分组 |
| 过期时间 | 通常不应超过套餐有效期 |
| 套餐令牌额度 | 用来限制这个子 Key 最多消耗多少套餐额度 |

::: tip 例子：团队怎么拆 Key
一个 Codex 套餐给 5 个成员共用时，不建议所有人共用同一个 Key。可以为每个成员创建独立令牌，例如 `codex-zhangsan`、`codex-lisi`。后续在使用日志或分组健康状态里，就能直接看出是谁消耗高、谁错误多、哪个项目需要限额。
:::

令牌列表会展示分组、倍率、可用模型和过期时间：

![令牌管理中的套餐令牌示例](/img/plans/tokens.png)

## 八、当前 14 个分组简介

下表按你在定价页看到的 `usable_group` 口径整理。当前共 **49 个模型、14 个可选分组**；具体可调用模型、倍率和可售状态以 [定价页](https://fishxcode.com/pricing?currency=CNY) 为准。

| 分组 | 分组说明 | 建议场景 |
| --- | --- | --- |
| `OpenRouter` | 【聚合官方｜OpenRouter】OpenRouter 官方聚合渠道；支持 Claude、Codex 等多系列模型，适合统一入口调用。 | 多系列模型统一入口 |
| `cc-max` | 【Claude Code｜官方 API】Claude Code 官方 API 渠道；稳定性优先，适合长期生产使用。 | Claude Code 稳定生产 |
| `cc_export` | 【Claude Code｜外接】Claude Code 外接专用分组；不限制客户端，适合自有工具或第三方客户端接入。 | 第三方客户端、自有工具 |
| `cc_max` | 【Claude MAX｜自建池】Claude MAX 自建号池分组；建议在 Claude Code 或官方插件中使用。 | Claude Code 或官方插件 |
| `claudemax_x20` | 【ClaudeMax｜x20 官方订阅】ClaudeMax x20 官方订阅账号分组；详情见 claudemax.fishxcode.com。 | 团队、重度 ClaudeMax |
| `claudemax_x5` | 【ClaudeMax｜x5 官方订阅】ClaudeMax x5 官方订阅账号分组；详情见 claudemax.fishxcode.com。 | 个人或轻量 ClaudeMax |
| `codex_plus` | 【Codex Plus｜外接】Codex Plus 系列号池分组；支持外接使用，适合高频代码与工具调用任务。 | 高频代码任务 |
| `codex_pro` | 【Codex Pro｜外接】Codex Pro 系列号池分组；支持外接使用，适合高频代码与工具调用任务。 | 更稳定的 Codex / GPT 编程任务 |
| `codex_sub` | 【Codex 订阅｜标准池】Codex 订阅系列分组；适合 Codex Plus / Pro / Team 号池，支持高缓存命中场景。 | Codex 订阅标准池 |
| `codex_sub_june` | 【Codex 订阅｜新版池】Codex 新版订阅系列分组；适合 GPT-5.x / Codex 系列模型，支持高缓存命中场景。 | 新版 GPT-5.x / Codex 模型 |
| `deepseek` | 【官方 API｜DeepSeek】DeepSeek 官方渠道；适合 DeepSeek 系列模型的稳定调用。 | DeepSeek 系列 |
| `default` | 【通用｜全站模型】默认按量分组，可调用站内开放模型；适合日常测试与普通请求。 | 日常测试、普通请求 |
| `gemini` | 【官方/高稳｜Gemini】Gemini 系列模型分组；适合多模态、长上下文与日常高频调用。 | Gemini、多模态、长上下文 |
| `grok` | 【专线｜Grok】Grok 系列模型分组；适合 xAI Grok 对话、推理与视觉任务。 | Grok 对话、推理、视觉 |

::: info 分组会动态调整
平台可能新增、下架或调整分组。文档用于解释选组逻辑，不替代控制台中的实时价格和权限展示。
:::

## 九、倍率和扣费例子

倍率只是在模型基础价格上再乘一个系数。倍率越低，通常同一模型同一请求的扣费越低。

| 模型基础消耗 | 分组或模型倍率 | 估算扣除 |
| --- | --- | --- |
| 10 额度 | `1x` | 10 额度 |
| 10 额度 | `0.5x` | 5 额度 |
| 10 额度 | `0.1x` | 1 额度 |
| 10 额度 | `2.5x` | 25 额度 |

更贴近调用的例子：

| 场景 | 估算方式 |
| --- | --- |
| 某模型基础消耗 8 额度，走 `1x` 口径 | 约扣 8 额度 |
| 同一请求走 `0.5x` 口径 | 约扣 4 额度 |
| 同一请求走 `2.5x` 模型倍率 | 约扣 20 额度 |
| 命中缓存的长上下文请求 | 缓存部分通常更便宜，最终以日志记录为准 |

::: warning 不要只看倍率
倍率不是唯一价格。不同模型的基础价格、输出倍率、缓存倍率差异很大；图片、视频模型还可能是固定价格。做成本估算时，请以定价页或控制台模型列表中的当前价格为准。
:::

## 十、使用后怎么核对实际消耗

调用后不要只凭估算判断费用，应该去 **控制台 → 使用日志** 查实际消耗。团队或套餐场景下，再结合 **分组健康状态** 判断是否是单个 Key、单个用户或整个分组的问题。

![分组健康状态示例](/img/plans/statistics.png)

分组健康状态适合回答这些问题：

| 问题 | 看哪个字段 |
| --- | --- |
| 哪个分组消耗最高 | 分组、消耗、请求数 |
| 是否只有某个成员异常 | 用户 / 令牌明细行 |
| 缓存有没有生效 | 缓存命中率、缓存 Tokens、缓存请求占比 |
| 是否是资源池问题 | 分组成功率、错误数、失败原因 |
| 是否是长上下文或大输出导致费用高 | 单次成功消耗、平均耗时、缓存 Tokens |

::: tip 例子：团队复盘
如果 `codex_sub` 分组整体成功率正常，但某个成员令牌的单次成功消耗明显高于其他成员，优先检查该成员是否在跑长上下文、大输出或重复任务。如果整个分组成功率下降，再考虑分组资源池、上游状态或套餐权限问题。
:::

## 十一、快速选择建议

| 使用场景 | 建议优先查看 | 原因 |
| --- | --- | --- |
| 不确定要用什么 | `default` 或控制台推荐分组 | 先验证工具配置是否正确 |
| Claude Code 稳定使用 | `cc-max`、`cc_max`、ClaudeMax 系列 | 更贴近 Claude Code 使用路径 |
| 自有客户端接 Claude | `cc_export` | 不限制客户端，适合第三方工具 |
| Codex / GPT 编程 | `codex_plus`、`codex_pro`、`codex_sub_june` | 面向代码和工具调用任务 |
| 多系列统一入口 | `OpenRouter` | 适合统一入口调用 Claude、Codex 等多系列模型 |
| Gemini CLI 或多模态 | `gemini` | 覆盖 Gemini 系列模型 |
| Grok 模型 | `grok` | 覆盖 Grok 对话、推理和视觉任务 |
| DeepSeek 模型 | `deepseek` | 覆盖 DeepSeek 官方渠道 |
| 团队或项目拆分 | 为成员或项目创建独立令牌 | 便于额度控制、成本分摊和日志排查 |

## 十二、排查消费或权限异常

遇到消费异常或模型不可用时，建议按这个顺序排查：

1. 确认当前 Key 绑定的令牌分组是否包含目标模型。
2. 确认余额、套餐有效期、日上限、月上限或总额度是否充足。
3. 查看 **控制台 → 使用日志**，按时间、模型、Token、分组、请求 ID 筛选。
4. 对比 [分组健康状态](/group-health)，判断是单个 Token 问题还是分组资源池问题。
5. 如果仍无法判断，联系支持时提供订单号、令牌名称、分组、模型名、请求时间和请求 ID。
